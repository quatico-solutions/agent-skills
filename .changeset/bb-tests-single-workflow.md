---
"@quatico-solutions/agent-skills": patch
---

Document the reworked `bb` CI in the test suite README: both platform jobs now always run and filter inside the job, rather than being path-filtered on the trigger with a mirrored skip workflow

<!--
bumps:
  skills:
    working-with-bitbucket-api: patch
-->
