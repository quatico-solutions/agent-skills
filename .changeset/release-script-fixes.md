---
"@quatico-solutions/agent-skills": patch
---

`pnpm run release` no longer half-applies on a missing token, and its tag is now annotated so `git push --follow-tags` actually carries it

<!--
bumps:
  skills: {}
-->
