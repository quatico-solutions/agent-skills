---
"@quatico-solutions/agent-skills": minor
---

Document the global `-R workspace/repo` flag in `working-with-bitbucket-api` so agents know `bb source` (and every other command) can target a repo other than the current directory's

<!--
bumps:
  skills:
    working-with-bitbucket-api: minor
-->
