---
"@quatico-solutions/agent-skills": patch
---

CI: report the required `bb-tests` check on PRs that do not touch the `bb` CLI, so unrelated PRs are no longer permanently blocked

<!--
No skill bumps: this changes CI configuration only, no skill content.
-->
